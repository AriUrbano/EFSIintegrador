git config user.name "María López"
git config user.email "maria@ejemplo.com"
git checkout main
git pull origin main
git checkout matias-sanchez-toranzo
git merge main
git add .
git commit -m ""
git push -u origin matias-sanchez-toranzo
